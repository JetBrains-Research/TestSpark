package org.jetbrains.research.testspark.data

/**
 * Enum class which contains states of tests in terms of preference of a user.
 */
enum class TestState {
    UNSPECIFIED,
    LIKED,
    DISLIKED
}